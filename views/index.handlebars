<h1>Smoothies!</h1>

<h2>Smooties that have not been eaten!</h2>

<ul>
  {{#each smoothies}}
    {{#unless devoured}}
      {{> smoothies/smoothie-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Smoothies that have been devoured!</h2>

<ul>
  {{#each smoothies}}
    {{#if devoured}}
      {{> smoothies/smoothie-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Smoothie</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Type of Smoothie:</label>
    <input type="text" id="ca" name="smoothieName">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Uneaten
  </div>

  <button type="submit">Add Smoothie</button>
</form>
