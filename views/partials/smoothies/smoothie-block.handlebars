<li>
	{{smoothieName}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-smoothie" data-id="{{this.id}}">DELETE!</button>
</li>
